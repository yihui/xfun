% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app.R
\name{httpd_start}
\alias{httpd_start}
\alias{httpd_stop}
\title{Start or stop the built-in HTTP server}
\usage{
httpd_start(host = "127.0.0.1", ports = 4321 + 1:10)

httpd_stop()
}
\arguments{
\item{host}{The host/IP address to bind to. Use \code{"127.0.0.1"} (default)
for local-only connections, or \code{"0.0.0.0"} for all network interfaces.}

\item{ports}{A vector of candidate port numbers. The first available port
is used.}
}
\value{
\code{httpd_start()} returns the base server URL invisibly (e.g.
\code{"http://127.0.0.1:4322/"}). \code{httpd_stop()} returns nothing.
}
\description{
\code{httpd_start()} starts a minimal HTTP server that listens on a local TCP
port and dispatches incoming requests to registered handler functions.
\code{httpd_stop()} stops the server and removes all registered apps.

In interactive R sessions the server integrates with R's event loop.
On Unix/macOS, incoming connections are handled immediately via
\code{addInputHandler} so the browser gets a response without the user pressing
Enter. On Windows, a task callback (\code{addTaskCallback}) is used, so a
response is delivered after the next top-level R expression is evaluated.
}
